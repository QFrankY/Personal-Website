{
	"db": {
		"name"      : "db",
		"connector" : "mysql",
		"database"  : "${MYSQL_DATABASE}",
		"host"      : "${MYSQL_HOST}",
		"username"  : "${MYSQL_USER}",
		"password"  : "${MYSQL_PASS}",
		"port"      : "${MYSQL_PORT}"
	}
}
